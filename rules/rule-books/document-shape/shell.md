# Use the same book-level layout

Rule id: `document-shape/shell`

**Rationale:** A fixed book layout makes the repository predictable to scan,
automate, diff, and maintain. Readers should not have to guess where the
enforceable rules stop and the maintenance material begins.
