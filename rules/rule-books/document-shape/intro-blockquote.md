# Keep the maintenance blockquote in `rules/README.md`

Rule id: `document-shape/intro-blockquote`

**Rationale:** That note explains why companion `Origins:` notes exist, so
maintainers treat them as provenance and change-impact tracking rather than as
decorative citations.
