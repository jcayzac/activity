# Derive companion paths from the full rule id

Rule id: `document-shape/sidecar-filenames`

**Rationale:** Path derivation must be mechanical and reversible. Using the full
rule id preserves uniqueness and lets tools or reviewers move directly between
`rules.md` and the matching companion file without a lookup table.
