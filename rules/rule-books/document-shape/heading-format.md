# Use plain section titles and slugged rule headings in `rules.md`

Rule id: `document-shape/heading-format`

**Rationale:** Plain section titles read naturally, while slugged rule headings
give stable anchors for reference and linking.
