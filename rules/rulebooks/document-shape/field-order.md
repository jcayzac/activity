# Split rule fields between `rules.md` and companion files

Rule id: `document-shape/field-order`

**Rationale:** Splitting fields by role keeps enforcement material fast to scan
while leaving explanation and provenance available when they are actually
needed.
