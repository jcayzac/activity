# Group rules by domain, not by source

Rule id: `domain-sections/grouping`

**Rationale:** Readers consult the book by topic, not by where a rule came
from. Source provenance belongs in `Origins`, not in section layout.
